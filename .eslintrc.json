{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "overrides": [
    {
      "files": ["src/lib/__tests__/**", "src/__tests__/**"],
      "rules": {
        "@typescript-eslint/no-explicit-any": "off"
      }
    },
    {
      "files": ["src/app/api/**"],
      "rules": {
        "@typescript-eslint/no-explicit-any": "warn"
      }
    }
  ],
  "rules": {
    "no-floating-promises": "off" // rule referenced but not installed; silence until plugin added
  }
}
